// get DOM elements
// set a variable to hold a random number between 1 and 20 using Math.Random()
// create a button event listener with an if... else statement:
//  if userGuess === randomNum, display "you win!"
//  else if guessesRemaining === 0, display "you're out of guesses"
//  else if userGuess > 20 or < 1, display "invalid number"
//  else if userGuess > randomNum, display "Too high"
//  else if userGuess < randomNum, display "Too low"
//  else break
// 